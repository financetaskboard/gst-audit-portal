# Node
node_modules/
npm-debug.log*

# Runtime state — contains your GST financial data, exclude from git
gst-portal-state.json

# Odoo credentials — keep these off GitHub
gst-odoo-settings.json

# Firebase Service Account Key — NEVER commit this to git
# This file gives full read/write access to your Firestore database.
# Download it locally from Firebase Console → Project Settings → Service Accounts.
# On Render.com, use the FIREBASE_SERVICE_ACCOUNT environment variable instead.
serviceAccountKey.json
*-firebase-adminsdk-*.json

# OS files
.DS_Store
Thumbs.db
